# Japanese Drill App — Project Summary

## What this is
A single-file HTML/CSS/JS Japanese learning app (`index.html`), hosted on GitHub Pages, built and used by Andrew and ~9 classmates for an in-person Japanese course, ahead of a Japan trip. No build step, no framework — deliberately kept as one file for simplicity (classmates just open it and it works). Currently ~3,530 lines.

**Working style established across many sessions:** draft/direct, Claude builds and validates. Every change gets syntax-checked, structurally validated, and functionally tested with real code before being presented as done — not just visually inspected. Claude asks before making judgment calls on ambiguous design decisions rather than assuming. Dead code/data gets cleaned up when found, not left to accumulate. When something is "roughly right but not quite," the instinct has consistently been to find and fix the actual root cause rather than patch the symptom.

## Architecture
- **Kana**: hiragana/katakana, each with a "selection screen" (row-based selection by gojuon section, confidence-shaded tiles, drill button)
- **Kanji**: 8 thematic sections (numbers-time, nature-body [now includes weather], directions-travel, food, places, people, general, verbs), each with its own tile-selection screen. Kanji always tests *meaning* (never sound) — options now show English prominently, romaji small/bracketed (deliberately different from vocab, which tests either).
- **Vocab**: 13 categories across Level 1, Level 2, and an "Extra" tier (Colours, Trip-prep). Every category has its own tile-selection screen (browse, individually select words, drill, scored). A "drill by meaning vs sound" toggle exists per-category with independent state (not shared/inherited across categories).
- **Read page**: numbers/times (one-tap, scored with red/green swatches), separated by a gold divider from a "random pool" section below (reading words L1/L2, reading words everything, random kanji) — those three are select-then-start with a shared toggle and Start button.
- **Scoring**: every drillable thing (kana, kanji, vocab, numbers, times) has a confidence score from -2 to +2, ±1 per answer for most things, ±0.1 per answer for numbers/times (aggregate, not per-item). Colours' tiles additionally get a border that shows hue+intensity matching the tile's own colour, on top of the standard red/green background shading everyone else uses.

## Terminology (settled, worth using consistently)
- **"Selection screen"** = interactive tile+score+drill screens (most things)
- **"Reference"** = reserved only for two genuinely pure-browse screens: the ordered Days-of-week list and the Minute-endings pattern table
- Code was fully renamed months ago to match this (function names, screen ids all say "Selection" not "Reference" where appropriate)

## Recent major work (this session, in order)
1. Full structural rebuild: Extra pill, gold dividers separating one-tap from select-then-start cards, Random Kanji correctly repositioned as a third select-then-start option (not an outlier)
2. Visual scoring: Numbers/Times swatches, Colours' border treatment (settled at 3px, "playful" not "insipid")
3. Fixed a real bug where returning from a finished drill left old tile selections visually hiding fresh score shading — now clears on return
4. Three fixes sourced from an external code review (Gemini): fixed a genuine dormant bug where two words sharing an English gloss could both register as "correct" in meaning-mode; made `KANJI_KEYS` derive dynamically from data instead of a separately-maintained list; fixed a stale "Random matching" label
5. **Kanji distractor overhaul** (the big one): distractors used to be hand-authored per-item, some genuinely invented with no backing data anywhere. Now generated dynamically from real pool items (same section, or full pool for "Random Kanji"), mirroring how vocab already worked. Required mining two new lookup tables (`KANJI_ROMAJI`, `KANJI_COMPOUND`) from existing verified data rather than writing new conversion logic. Removed the now-dead hand-authored options (145 items, ~23KB).
6. **Kanji data realignment** (just completed, found by Andrew, not Claude): 47 kanji items had their English meaning, romaji, and audio reading all describing *different things* — e.g. character shown = 山 ("mountain"), but romaji/audio said "fujisan" (specific to Mt Fuji, from the compound 富士山). Fixed so all three (character shown, English, romaji, audio) now consistently describe the *character's own standalone meaning*, with the compound word preserved as a genuine gold "(example: ...)" note rather than secretly being the tested content. **Note: a real mid-fix bug occurred here** — a scoping mistake caused 41 of these items' audio to initially stay on the old (wrong) reading while corrupting the romaji table; this was caught by re-running verification (not by assuming success) and properly repaired. Fully verified clean afterward.
7. Fixed "eat"/"drink" to say "to eat"/"to drink" matching the Verbs section's established convention

## Recent major work (new session)
8. **Compound-word promotion**: the kanji realignment (item 6) correctly made every single kanji test its own standalone meaning, but Andrew noticed a side effect — some compounds that used to be (loosely, inconsistently) testable content became nothing more than passive "(example: ...)" text after an answer, with no romaji/reading/audio anywhere. Concretely: 電車 (train) disappeared from the app entirely once 電/車 were correctly split into "electric"/"car-vehicle"; 富士山 (Mt. Fuji) and 日本人 (Japanese person) had the same problem. Decision made with Andrew: promote genuinely distinct-meaning compounds into real standalone drillable items (own char/english/romaji, same section as their parent kanji, parent kanji items untouched), while parent kanji and compound coexist as separate drills. Added 23 clean compounds (富士山, 日本人, 電車, 日本, 大学, 小学校, 中学校, 上手, 地下鉄, 左折, 右折, 東京, 西口, 南口, 北口, 東京駅, 山手線, 病院, 時計, 午前, 午後, 今年, 今日) plus 5 verb-form compounds reframed as "to ___" to avoid duplicate-gloss collisions with their base kanji (開く→"to open", 閉まる→"to close", 押す→"to push", 引く→"to pull", 止まれ→"Stop! (sign)", the last being imperative/sign form rather than dictionary form — flagged as a judgment call, not a strict "to stop"). Deliberately deferred: 学校/銀行/お店 (school/bank/shop), where the compound's English would be a word-for-word duplicate of the already-tested single kanji (校/行/店) with no verb-form escape hatch — left as-is pending Andrew's own testing experience; and 千円/三分 (pure number+counter demonstration phrases, not real standalone vocabulary) — skipped for now. Verified via a Node harness that evals the DATA/KANJI_ROMAJI/KANJI_READINGS objects directly: no duplicate `answer` strings within any kanji section, no duplicate `char` values, every kanji/survival item resolves in both lookup tables. Total kanji items: 145 → 173.
9. **Found in passing, fixed**: `KANJI_ROMAJI`/`KANJI_READINGS` still had "eat"/"drink" as their lookup keys even though item 7 had already renamed the tested answer to "to eat"/"to drink" — meaning those two items showed a blank romaji caption whenever drawn as a distractor option. One-line fix, unrelated to the compound-promotion work but caught by the same verification pass.
10. **School-word placement fix**: the initial compound promotion placed each new item in whatever section its *parent* single kanji lived in (大→Nature/Body, 小→Nature/Body, 中→Travel) — mechanical, not thematic, and it left 大学/小学校/中学校 scattered across two sections with nothing to do with schooling. Andrew caught it. Moved all three into `kanji-general` (was a small 8-item section; also nicely balances section sizes: general 8→11, nature-body 17→14, travel 63→62 after removing 中学校). No lookup-table changes needed since `KANJI_ROMAJI`/`KANJI_READINGS` key off char+answer, not section.
11. **East exit added for symmetry**: Andrew noticed the original compound table (predates this session) tied 東 (east) to 東京 (Tokyo) as its example, while 西/南/北 were each tied to their own exit compound (西口/南口/北口) — so the promotion carried an asymmetry forward: Tokyo + 3 exits instead of 4 exits. Andrew wanted both Tokyo *and* east exit kept, since both have real value. Added 東口 (higashiguchi, "east exit") as a second promoted compound off the same 東 parent — same pattern as train/car both coming from 電/車. Total kanji items: 173 → 174.
12. **Real bug found and fixed: "Drill missed only" had no answer options for any kanji/survival item**. Root cause pre-dates this session (introduced by item 5's distractor overhaul, just never surfaced until Andrew hit it on 日本人 tonight): `drillMissed()` rebuilds missed items by pulling the raw object straight out of `DATA`, but kanji/survival items never carry a static `options` array — options are only ever generated fresh at drill-start time via `buildKanjiOptionsFromPool`, then discarded. The one path that *does* preserve a full working object for redrill (`missedItems`, populated in `choose()`) only fires for `disp === 'number' | 'word' | 'time'` — kanji/survival was never included, so every kanji miss fell through to the broken raw pull. Fixed by regenerating options for kanji/survival items inside `drillMissed()` itself, same call as `drillKanjiSelection` uses. Verified by actually driving `choose()` → `drillMissed()` through a Node harness (not just reading the code): confirmed kanji items now get 4 populated options, and re-confirmed kana (static options) and vocab words (already-working `missedItems` path) still work with no regression.
    - **Related, narrower, still-open issue found while investigating (not fixed)**: `missed` tracks by bare character only, not character+meaning. Exactly 4 characters currently do double duty across sections with different tested meanings — 火 (Tuesday / fire), 水 (Wednesday / water), 木 (Thursday / tree-wood), 日 (Sunday / sun-day) — so getting one meaning wrong and using "Drill missed only" would technically pull in both entries for that character. Confirmed none of tonight's 24 new items caused this; it's pre-existing and narrow. A proper fix means tracking char+answer together, which touches a few other spots — worth a deliberate look, not a drive-by fix.
13. **Kanji section rebalance**: Travel had grown to 63 items (outstanding item 7) — by far the biggest section, would take forever to drill as a whole. Andrew proposed the fix directly: merge Weather into Nature (they pair naturally — rain/sun/cloud/hot/cold alongside mountain/river/fire/tree — and Nature's pill was already just labelled "Nature", so no rename needed), and carve a new "Places" section out of Travel for the genuine location/facility nouns that had been living there for no thematic reason. Also moved 男/女 (male/female) into People and 上手 (skilled/good at) into General — both had been stuck in Travel purely because their parent single kanji (行, 上) happened to live there, same "mechanical placement" issue as the school words in item 10. 8 sections total both before and after (Weather's pill slot became Places' pill slot, no UI/CSS layout change needed since the pill grid is a fixed 4×2).
14. **Schools also moved to Places**: Andrew followed up — since 校 (school) and 院 (institution) already sit in Places as standalone place-nouns, 大学/小学校/中学校 (university/elementary/middle school) belonged there too rather than in General, where they'd only landed because of item 10's "follow the parent kanji" mechanical rule. Moved all three. **Final section sizes**: Numbers 48, Nature 26 (was 17), Places 19 (new, was 0), Travel 44 (was 63), Food 10, People 11 (was 9), General 9 (was 11), Verbs 7 (base count before item 15). Total unchanged at 174 — both moves were reorganization, not content changes. Andrew's read: "some feel a bit empty and some are a bit full" but the groupings work logically — not chasing perfectly even numbers, thematic sense wins.
15. **Verbs section made into a computed cross-section pool, resolving outstanding item 6**: Andrew came back to the parked "drill all verbs I know" idea and asked for it directly, with one hard constraint — no duplicate cards anywhere. Two readings of "no duplicates" were possible (remove verbs from Food/Travel's own screens entirely, vs. same underlying item reachable from two screens with a single source of truth) — asked Andrew directly rather than guessing; he confirmed the latter, same relationship Random Kanji already has with every kanji. Implemented as `getKanjiSectionPool(sectionKey)`: for every section except Verbs it just returns the plain `DATA[...]` array unchanged; for Verbs it scans every kanji-* section fresh each call for `answer.startsWith('to ')` and returns those items tagged with their true origin as `groupKey` (same convention `buildRandomKanjiPool` already uses), so the drill badge shows where a verb really lives. Nothing is copied into a second DATA array — Food and Travel's own tile grids are 100% unchanged (still 10 and 44 items respectively, still show 食べる/飲む/開く/閉まる/押す/引く as normal selectable tiles). The one deliberate exclusion: 止まれ ("Stop! (sign)", item 8's imperative-not-infinitive judgment call) correctly doesn't match `"to "` and stays out of the Verbs pool — a nice confirmation that decision was made correctly. Verbs pool: the original 7 general verbs (見る/聞く/話す/読む/書く/来る/休む) + 食べる/飲む from Food + 開く/閉まる/押す/引く from Travel = 13 total. Verified end-to-end via Node harness: Verbs pool size and origin tags correct, Food/Travel pools and deck sizes unaffected, Random Kanji's pool and 174-item total unaffected (confirming no hidden double-weighting), syntax clean.
16. **`missed` tracking fixed to key on char+answer, resolving outstanding item 8**: `choose()` now builds `missedKey = char + '|' + answer` instead of tracking bare `char`, so characters that do double duty across sections with different tested meanings (火 Tuesday/fire, 水 Wednesday/water, 木 Thursday/tree-wood, 日 Sunday/sun-day) no longer get conflated. Updated all 3 consumers: `drillMissed()`'s regular-DATA scan and its `missedItems` filter both now match on `q.char + '|' + q.answer`; `showResults()`'s "Focus on these" display now splits the composite key back to just the char portion before rendering, so the visual output is unchanged. Verified end-to-end: simulated getting 火 wrong as "Tuesday" specifically, then confirmed `drillMissed()` returns only that one entry (not also 火/fire from Nature) — the exact scenario the bug described. Also confirmed the results-screen display renders clean characters ("火 駅"), not raw composite keys.
17. **Vocab distractor plausibility fixed, resolving outstanding item 1**: traced the actual UI first — each vocab option button shows both fields at once (romaji prominent + English small, or vice versa in meaning mode), and the drill prompt shows the raw kana spelling directly. So the "first letter/sound" shortcut classmates found was real and specific: with pure-random distractors from the ~270-word pool, wrong options usually looked nothing like the correct answer (very different starting letter, very different length), so students could eliminate 3 of 4 options on sight without recognising the word at all. Fixed `pickDistractorItems` (the single function feeding all three vocab distractor call sites — category drilling, word-reading-everything, and random-level reading — so one fix covers all of them) to score every candidate's plausibility (same starting letter = +3, length within 3 chars = up to +3) and bias selection toward the more confusable ones, while still shuffling first so ties resolve randomly and repeated drills don't always show the same decoys. Measured the actual improvement with a 3000-trial Node harness comparing old vs. new: **the fraction of questions with at least one distractor sharing the correct answer's first letter went from 19% to 98%**, and average distractor/answer length gap dropped from 3.7 to 1.1 characters. Confirmed no regressions: 1000 trials in both sound and meaning mode with zero duplicate-option collisions, every one of the 15 vocab categories (down to the smallest at 4 items) still returns a full 3 distractors, and the real `drillVocabTileSelection`/`buildWordReadingDeck` functions still produce fully-populated decks end to end. No words from outside the existing pool were introduced, matching the constraint from when this item was first agreed.

## Recent major work (continued)
19. **Version stamp added**: a small grey line below the "Any problems: Andrew..." text on the Read screen, styled to match the card-footer font (`.group-count`: 12px/600/`var(--text-dim)`). Format `v[YYMMDD].[NNN]`, manually bumped by one on every future change so Andrew can always tell which build he's looking at on his phone. Started at `068` (his actual commit count at the time), currently `v260721.069`.
20. **Drill-resume fix corrected: sessionStorage → localStorage, resolving the real-device failure of item 18**: Andrew tested v068 (item 18's fix) on his phone and confirmed it made no difference at all — first "back" does nothing (existing history-trap working as designed), but the *second* back press closes the app outright, and the drill starts from nothing next time with no resume prompt ever appearing. Root cause: sessionStorage is cleared the moment a browsing session genuinely ends (spec-mandated), and that second back press *is* a genuine close, not just backgrounding - so the saved state was wiped before any reload could read it back. Switched all three functions (`saveDrillSession`/`clearDrillSession`/`tryRestoreDrillSession`) from sessionStorage to localStorage, which has no session-lifetime limit and survives a real close the same as a backgrounded kill. Verified with two genuinely separate Node process invocations sharing a disk-backed fake localStorage (as durable a simulation of "real device, real close" as a Node harness can get): confirmed the OLD sessionStorage-based code reproduces Andrew's exact symptom (empty deck, no resume prompt, nothing shared between the two processes), while the NEW localStorage-based code correctly restores deck/current/score/etc. across the same two-process boundary. Only tradeoff: a resumable session is no longer scoped to "same session," so it could technically be days old - not mitigated with an expiry, since the existing confirm() prompt already lets Andrew decline a stale one cleanly; flagged in code comments rather than silently added.

21. **Drill-resume fix confirmed working on Andrew's phone (v069)** — resolves item 3/20 for real. Two small refinements requested straight after: (a) 1-hour auto-expiry added (`DRILL_SESSION_MAX_AGE_MS`, checked against a new `savedAt: Date.now()` field written by `saveDrillSession()`) — a session older than that is silently discarded with no prompt, since drills are short and real interruptions are noticed within seconds, not resumed an hour later; (b) resume prompt wording changed to "It looks like your last session was interrupted. Resume where you left off?" (previously said "drill"). Both verified with a Node harness across three ages (30/59/90 min) — 30 and 59 both still prompt and restore correctly, 90 discards silently as intended. Flagged and confirmed with Andrew: the browser's native confirm() popup always prepends its own header line (e.g. "yourname.github.io says") above the message text, which JS cannot suppress for security reasons — Andrew confirmed the wording fix alone is enough, no need to build a custom on-page popup to remove that header. Now v260721.070.

22. **School/bank/shop duplicate-gloss compounds resolved (outstanding item 5), via the existing "also written:" mechanism instead of promotion**: Andrew proposed reusing the gold "also written:" box already used for vocab reading (e.g. ぎんこう shows "also written: 銀行" up front, visible the whole question) rather than promoting 学校/銀行/お店 into separate drillable cards (which would have duplicated the English gloss already tested by 校/行/店). Implemented by giving those items an `altForm` field (same field vocab already uses) instead of only relying on the after-answer "(example: ...)" parenthetical. While auditing all 54 `KANJI_COMPOUND` entries against their actual tested answers to find every genuine word-for-word duplicate (not just the three originally flagged), found 4 more: 火/水/木/日 (tested as Tuesday/Wednesday/Thursday/Sunday) each have a compound (火曜日/水曜日/木曜日/日曜日) that means exactly the same day-of-week, with no added meaning — same issue, never previously noticed. Also corrected a misstatement made earlier in this conversation: 行 tests with the answer text "go / bank (銀行)" — meaning "bank" was already sitting inside the single kanji's own answer, a more direct duplicate than I'd described. Andrew confirmed extending the fix to all 7 (校/店/行/火/水/木/日) once shown the exact list, not just the original 3. For each: added `altForm` in `DATA`, removed the now-redundant entry from `KANJI_COMPOUND` (47 entries left, single source of truth is `altForm` for these), and updated `choose()` to skip its `(example: ...)` parenthetical whenever `altForm` is already set (avoids showing the same compound twice on one card). Verified with a Node harness running the real `renderQuestion()`/`choose()` function bodies: confirmed the gold box now shows correctly and up front for all 7, the redundant after-answer note is suppressed for exactly those 7, an untouched control item (山/mountain → still shows "(example: 富士山)" after answering, no gold box) is unaffected, and existing vocab `altForm` usage (e.g. ぎんこう/bank) still works unchanged. Now v260721.071.

23. **Keyboard shortcuts added for laptop/desktop drilling (Gemini's suggestion)**: every question always renders exactly 4 option buttons, in the same order they're displayed, across all 5 rendering paths in `renderQuestion()` (word reading, number reading, time reading, reversed kana, and normal kanji/kana). Added a subtle numbered badge (1-4, `.opt-num` - small, low-opacity, top-left corner, matching the app's existing "quiet" visual language) to each option in all 5 paths, plus a single global `keydown` listener (`handleDrillKeydown`) registered once at startup: digits 1-4 click the matching option when a question is unanswered, Space or Enter clicks "Next →" once it's answered. Scoped tightly to avoid side effects - only active while `screen-drill` is the visible screen, ignores the keypress entirely if Ctrl/Cmd/Alt is held (so it doesn't hijack real browser shortcuts), and does nothing on mobile since taps don't fire keydown events at all. Verified with a Node harness built around a minimal fake DOM (real click()/classList/appendChild semantics, not just log inspection): confirmed all 4 badges render correctly across a sample path, pressing a digit clicks the correct option and produces the correct correct/wrong visual marking and feedback text, a second digit press after answering is correctly ignored, Space does nothing before answering but correctly advances `current` after, the whole handler is inert when a different screen is visible, and Ctrl+digit is ignored without calling `preventDefault`. Now v260721.072.

24. **PWA manifest merged into canon**: brought over from the separate experimental chat (`manifest.json` + the 8-line `<head>` snippet), after confirming Andrew's phone testing there showed manifest-only (no service worker) doesn't fix the back-button issue but is still worth keeping for the cleaner, chrome-less install experience. `manifest.json` colors (`#0f0f0f`) confirmed to match the app's own `--bg` variable exactly. The four icon files it references (`icon-192.png`, `icon-512.png`, `icon-512-maskable.png`, `apple-touch-icon.png`) couldn't be transferred from the other chat - image uploads kept arriving as pasted previews rather than real file attachments, so no file data was ever accessible - so recreated fresh via SVG and rasterized to the four required sizes via ImageMagick. First attempt was a stylized "日"-inspired brush-stroke monogram, but it read more like a bold letter "H" than 日 (it was missing the top/bottom edges that close 日 into a full rectangle) - Andrew asked for the real kanji instead. Re-rendered as the actual 日 character set directly in Noto Sans CJK JP Bold (the sandbox's closest match to the app's own BIZ UDPGothic, which isn't installed as a system font here), gold on the app's dark background, sized/centered by eye since the SVG renderer used here doesn't honor `dominant-baseline: central` the way a browser would. Apple's touch icon deliberately left full-bleed/unrounded (no pre-baked corner rounding) rather than matching the rounded preview Andrew showed, since iOS applies its own corner mask and double-rounding is a common real-world PWA mistake. New `icons/` folder added alongside `index.html` and `manifest.json` - first departure from the pure single-file approach, but unavoidable for a working manifest. Verified: `manifest.json` is valid JSON, main JS block still syntax-checks clean, and every file the manifest/head snippet reference actually exists at the expected relative path. Now v260722.073.

25. **"space" hint added to the Next/Results button**: Andrew tried the Space-to-advance shortcut and found it worked but wasn't discoverable - nothing on screen suggested it existed, unlike the numbered option badges. Added the same quiet treatment (`.kbd-hint` - 10px, `var(--text-dim)`, ~55% opacity) as a small lowercase "space" label next to the button text, for both the "Next →" and "See results →" states (single button, same `nextBtn.innerHTML` assignment covers both). Verified with a Node harness exercising the real `choose()` function body directly: confirmed the hint renders correctly attached to both button-label variants. Repositioned to top-left corner right after, to match the numbered option badges exactly (`position: absolute; top: 4px; left: 6px`, same values as `.opt-num`) - required adding `position: relative` to `.next-btn` itself, same relationship `.opt` already has with `.opt-num`. Now v260722.076.

26. **"Drill by meaning instead of sound" relabeled to "Highlight the meaning instead of sound"**, all 3 occurrences (kana toggle, and both vocab-category toggle cards). Text-only change - the underlying `drillByMeaning` variable/logic untouched. Now v260722.077.

27. **Settings screen built, along with 3 of the 4 toggles previously parked**: Andrew asked for these built now, on the basis that they can't change anyone's experience unless they actually visit Settings and flip something themselves. Built:
    - **Foundation**: `appSettings` object (`theme`/`showCustomContent`/`hintPenalty`) persisted to a new `jpDrillSettings` localStorage key, defaults exactly matching pre-existing behaviour (`dark`/`true`/`false`), `applyAllSettingsUI()` called once at startup.
    - **New Settings screen** (`screen-settings`), reached via a new gear-icon button in the header, reusing the existing `.group-card`/`.toggle` pattern already used for "Highlight the meaning instead of sound" so it looks native to the app rather than bolted on.
    - **Header layout**: the "日本語 drill" logo moved off the pill row onto its own heading line above, freeing the space it used to occupy for the new settings button. All 5 nav pills (Hiragana/Katakana/Kanji/Drill/Read) are completely untouched - same elements, same order, same count - so `showScreen()`'s index-based active-pill logic needed no changes; it only gained a new special case so `showScreen('settings')` activates the gear button instead of falsely landing on "Drill" (the previous `else` catch-all would have wrongly marked Drill active for any unrecognised screen id).
    - **Light theme**: a `:root[data-theme="light"]` CSS override block redefining every colour-related variable (dark values in the plain `:root` stay untouched/default). A fresh warm-off-white/gold palette was designed for this pass rather than a literal invert, since several dark-mode colours (the pastel green/red swatches especially) would have had poor contrast on a light background as-is.
    - **Show custom content**: implemented as "grey out + disable" rather than hide, per Andrew's request to keep it small. A `.pill-disabled` class (dimmed + `pointer-events: none`) toggled on the Kanji nav pill and the vocab "Extra" level pill, backed by a guard at the top of `tryResumeKanji()` and inside `setMatchLevel()` (only for the `'extra'` case - other levels unaffected) as a backstop against keyboard/other activation paths that CSS `pointer-events` wouldn't catch.
    - **Hint penalty**: `recordKanaResult()` now checks `appSettings.hintPenalty` and `hintUsedThisQuestion` together - a correct answer only gets half credit if both are true; wrong answers always cost the full amount regardless. Required two follow-on fixes, both real bugs caught by testing rather than assumed away: (1) `kanaScoreClass()` switched from strict equality to the range-based bucketing already flagged as necessary, so a 0.5 value shades correctly instead of maxing out the tile; (2) the score-rounding step itself (`Math.round(next * 10) / 10`) was rounding 0.05 straight back up to 0.1 due to how `Math.round` handles the 0.5 boundary at that precision - silently cancelling the whole point of the half-credit for numbers/times. Switched to 2-decimal rounding to fix it.
    - **Verified thoroughly via Node harness** (not just read through): settings defaults, `toggleTheme`/`toggleCustomContent`/`toggleHintPenalty` each independently, `tryResumeKanji()` and `setMatchLevel('extra')` correctly blocked only when the setting is off, `showScreen('settings')` vs `showScreen('kanji-selection')` producing the right active/inactive states on both the gear button and the nav pills, and the full hint-penalty scoring matrix (penalty off = no change ever; penalty on + hint used + correct = half credit; penalty on + hint NOT used + correct = full credit; wrong = always full penalty regardless of hint/setting) including the numbers/times 0.05 case specifically, both before and after the rounding fix.
    - Now v260722.078.

28. **Two follow-on refinements to item 27**: (a) nav-pill row moved back to right-justified (`.nav-row` gets `justify-content: space-between`), giving clean separation between the new settings gear button on the left and the pill group on the right - Andrew confirmed the light theme colours read well too, no changes needed there. (b) "Random kanji" on the Read page (`#read-pool-kanji`) brought into the show-custom-content toggle's scope, which had been missed - now greyed out/disabled the same way as the Kanji nav pill and Extra tier pill, with guards in both `selectReadRandomPool()` (blocks selecting it while off) and `startReadRandomDrill()` (blocks starting it even if it was selected *before* the setting was switched off - closes a narrow edge case cheaply, one extra `&&` condition). Verified via Node harness: card greys out/re-enables correctly, selection is blocked while off and allowed while on, and the "already selected, then disabled" edge case no longer starts a kanji drill. Now v260722.079.

29. **Show-custom-content toggle temporarily hidden from Settings, not deleted**: Andrew's instinct was that properly fixing the pool-leakage issue (item above) would get messy, and he'd rather park it than ship something half-working - but also didn't want the code thrown away just to be rebuilt later. Middle ground: the toggle card is hidden (`display:none`, HTML comment explaining why), while `toggleCustomContent()`, `applyCustomContentVisibility()`, the `.pill-disabled` CSS, and every guard (`tryResumeKanji`, `setMatchLevel`, `selectReadRandomPool`, `startReadRandomDrill`) all stay exactly as built. Also added a one-line force-on (`appSettings.showCustomContent = true` right after settings load, overriding whatever's in storage) so nobody who'd already switched it off during testing gets stuck with content hidden and no visible toggle to undo it with. Verified with a Node harness simulating both a fresh user and someone with a previously-saved `false` - both correctly resolve to content visible. Re-enabling this later is just deleting the hide comment/style and the one force-on line. Now v260722.080.

30. **"Work offline" toggle built - 4th and final settings toggle**, prompted by real urgency: v080 is with users and Andrew had 2 hours of underground travel coming up the same day, a genuine no-signal test window. New file `sw.js` sits alongside `index.html`/`manifest.json`/`icons/`, registered/unregistered entirely by the app based on the new toggle - the service worker never self-registers.
    - **Strategy**: network-first, cache-fallback, for every GET request. Online, everyone always gets the freshest version (preserving the app's core "instant reload to see changes" strength) - the cache is only ever consulted when the network request actually fails. A precache list covers `index.html`, `manifest.json`, and the 4 icons; anything else (notably the cross-origin Google Font request) gets cached opportunistically the first time it's fetched.
    - **Versioning**: `CACHE_VERSION` inside `sw.js` is a separate constant from the page's own version stamp, but must be bumped in lockstep with it from now on - the `activate` handler deletes any cache whose name doesn't match the current `CACHE_VERSION`, which is what stops an offline-enabled user ever getting stuck on a stale cached version forever. **This is now a second thing to remember on every future version bump**, alongside the visible stamp - forgetting it doesn't break anything immediately, it just means the old cache lingers one extra cycle.
    - **Toggle lifecycle**: turning it on calls `navigator.serviceWorker.register('sw.js')`; turning it off actively calls `.unregister()` on every existing registration *and* deletes every `jp-drill-*` cache (not just stops touching them) - chosen deliberately so "off" is a real, clean off. Also re-synced once at startup in case of drift.
    - **Verified as much as a non-browser environment allows**: `sw.js` syntax-checked clean; registration/unregistration logic tested with mocked `navigator.serviceWorker`/`caches` - confirmed toggling on registers, toggling off unregisters every registration and deletes only the app's own caches (unrelated cache names untouched), and the whole feature no-ops safely if `serviceWorker` isn't supported at all. The `fetch` handler's branching logic was extracted and dry-run against mocked `fetch`/`caches`/`Response` covering all four real scenarios: network succeeds (returned + cached), network fails with an exact cache match (served from cache), network fails with no match on a navigation request (falls back to cached app shell instead of a browser error page), network fails with no match on a non-navigation request (errors cleanly rather than wrongly handing back HTML for an image request).
    - **What can't be verified this way, flagged honestly**: real offline behaviour, real caching persistence, and whether a future update actually reaches an offline-enabled user all need an actual device and an actual loss of signal - not something a Node script can stand in for. Andrew's underground commute is exactly that test.
    - **Operational note**: the toggle needs to be switched on and the app given a moment to actually install/cache *while still online*, before signal is lost - shipping this code alone doesn't cache anything by itself.
    - Now v260722.081, with `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-081` to match.

31. **Real-world offline test (item 30) partially failed - root cause found and fixed**: Andrew's actual underground test showed the app working offline on the way out, but "page couldn't be found" on the way back when it needed a fresh, fully-offline load. Root cause: `cache.addAll()` in the `install` handler is all-or-nothing - if even one of the 7 precache URLs failed to fetch (a 404, a file not actually deployed live yet, momentarily flaky signal during install), the *entire* call rejects and **nothing** gets cached, not even `index.html` itself, silently, since the rejection was being swallowed on purpose (to avoid blocking install). That fully explains the asymmetry: outbound, the tab likely stayed open in memory or had enough intermittent signal for the opportunistic per-request caching (in the `fetch` handler) to paper over the gap; on the way back, a genuinely cold offline reload had no `index.html` cached anywhere to fall back to. Fixed by switching from one `cache.addAll(PRECACHE_URLS)` call to caching each URL independently (`cache.add(url).catch(() => {})` per item, all run in parallel) - now one bad asset can no longer take `index.html` down with it. Verified via a Node harness extracting the real `install` handler and running it against a mocked `cache.add` with different URLs deliberately failing: confirmed `index.html` still caches successfully whether an icon fails, the manifest fails, or nothing fails - the only case where it doesn't cache is if `index.html` itself is truly unreachable, which is correct (there's nothing to fall back to in that case). Now v260722.082, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-082` to match.
    - **Confirmed with Andrew**: all files (`icons/`, `manifest.json`, `sw.js`) were genuinely live on GitHub Pages at the time of the failed test - so a missing file wasn't the cause. That points to the failure being a transient one instead (a single flaky fetch during install, e.g. GitHub Pages' CDN not having fully propagated one asset yet, or a momentary signal drop during the precache step itself) - exactly the class of one-off failure the `addAll` atomicity bug would turn into a total precache wipeout. The fix stands regardless of which asset flaked or why; not yet re-tested for real, pending Andrew's next signal-loss opportunity.

32. **First dedicated redundant-code cleanup pass**, done at Andrew's request ("check for redundant code" - something he'd flagged he'd want "at some point" back when the show-custom-content toggle was hidden rather than deleted). Audited every named function (113), every CSS class/id, and the kanji lookup tables (`KANJI_COMPOUND`, `KANJI_ROMAJI`, `KANJI_READINGS`) for anything unreachable. Good news first: no dead functions at all, and `KANJI_COMPOUND`/`KANJI_ROMAJI`/`altForm` were completely clean - zero orphaned entries, zero overlap between the altForm and KANJI_COMPOUND duplicate-gloss mechanisms (confirming item 22's migration was done cleanly). Three real, safe things removed:
    - `.section-label` CSS class (6 lines, plus its `:first-of-type` variant) - styled but never applied to any element anywhere in the app.
    - Six HTML `id` attributes sitting on purely decorative toggle-knob `<div>`s (`direction-toggle-ref-hira`, `direction-toggle-ref`, `hiragana-answers-toggle-ref`, `kanji-hiragana-answers-toggle`, `vocab-tile-drill-mode-toggle`, `drill-mode-toggle-l`). Traced how the toggle visual actually works: the knob's appearance is driven entirely by a `.group-card.selected .toggle` CSS rule keyed off the *parent* card's own id + `.selected` class (confirmed by checking every toggle function - `toggleDirection`, `toggleHiraganaOnKatakana`, `toggleHiraganaOnKanji`, `toggleVocabTileDrillMode`, `toggleDrillByMeaning` - all of which `getElementById` the `-card` id, never the child `-toggle` id). The child ids were never read by anything.
    - 45 of `KANJI_READINGS`'s 219 entries were unreachable - the table is only ever queried by a real item's own `char+answer` (two call sites total, both confirmed), but had accumulated a pile of entries keyed by compound-word phrases (学校, 銀行, 危険, 入口, 二階, 食堂, etc.) under a comment claiming they were "needed... for distractor options" - traced `buildKanjiOptionsFromPool` directly and confirmed distractor options are *always* built from real pool items' own char/answer, never a compound phrase, so that comment's premise no longer holds (most likely stale from before item 5's kanji-distractor overhaul, which switched distractors to real-pool-only). Also caught a few literal stale duplicate keys left over from past answer-text edits (e.g. both `'食堂|canteen'` and `'食堂|canteen / cafeteria'` existed - only the second, matching the item's current answer text, is real).
    - **Caught a mistake in my own first pass**: initially also removed `'間|interval / space (時間)': 'かん'` and `'午|noon (午前/午後)': 'ご'`, wrongly assuming they were duplicates of other orphaned shorter-key entries. A recount (expected valid-entry total didn't match actual) caught the discrepancy before presenting anything as done - both keys do match real DATA items (`間`/`午` with those exact parenthetical answer strings) and were restored. Final count: 174 valid entries, re-verified as 0 orphaned and 0 conflicting duplicate keys.
    - Verified throughout: syntax-checked the full extracted script, recomputed the orphan/duplicate audit from scratch after every edit (not just once), and confirmed via grep that the toggle functions never referenced the six removed ids in the first place (so no behavioural risk there). `sw.js`'s `CACHE_VERSION` bumped alongside, since `index.html`'s content changed and any offline-enabled user's cached copy needs to be treated as stale.
    - Now v260722.083, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-083` to match.

33. **"Specific time" lookup tool added to the Read page, under Reading times**: Andrew asked whether a free-input "enter a time, see the Japanese" tool was practical, describing a layout of a time entry field, AM/PM buttons to the right, and at/until/around buttons on the left. Turned out to be a strong reuse case rather than new linguistic work - the existing time-reading drill already had a complete, general `timeToKana`/`timeToRomaji` conversion engine (any hour 1-12, any minute 0-59 with full irregular ふん/ぷん voicing and the 30→はん special case, AM/PM or omitted, and a frame word), so this was almost entirely a UI job on top of proven logic. Confirmed with Andrew to also include "from" (kara) as a 4th frame button alongside at/until/around, since the engine already supported it. Built as a new static (non-drilling) `.group-card` directly under the existing "Reading times" card: two `<select>` dropdowns for hour (1-12) and minute (0-59, zero-padded), 4 frame toggle buttons (at/until/around/from) and 2 AM/PM toggle buttons reusing the existing `.level-pill`/`.level-pill.active` styling, and a live output box showing both the kana reading and its romaji, updating instantly on any change via `updateTimeTool()`. Deliberately used separate hour/minute selects rather than a native `<input type="time">`, since native time pickers on some devices/locales bake in their own AM/PM control that would have fought with the dedicated buttons Andrew wanted. Frame and AM/PM buttons behave as toggles - clicking the active one again returns to "none"/unspecified, matching how the underlying engine already treats both as optional. Verified via a Node harness extracting the real `timeToKana`/`timeToRomaji`/`populateTimeToolSelects`/`setTimeToolFrame`/`setTimeToolAmpm`/`updateTimeTool` function bodies against a mocked DOM (`<select>` with real default-selection semantics, `classList`): confirmed correct defaults (hour 3, minute 0), frame/ampm single-select-with-toggle-off behaviour, live output matching `timeToKana`/`timeToRomaji` directly (including irregular cases like 6:30→はん and 4:10→じゅっぷん), and correct 12/60-option population with zero-padded minute labels. One harness bug caught and fixed along the way (a fake `<select>` that didn't default its value to the first option like a real browser does) - confirmed as a test-fixture gap, not an app bug, before re-running clean.
34. **Contact line moved from the Read page to Settings**: "Any problems: Andrew 07973 835165" used to sit above the version stamp at the bottom of the Read screen; moved to the bottom of the Settings screen instead (version stamp stayed on the Read page). Simple relocation, no logic involved - verified the text now appears exactly once in the file, in its new location, and the main script still syntax-checks clean.
    - Now v260722.084, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-084` to match.

35. **Specific-time tool refined per Andrew's feedback on the first version**: three changes. (a) Defaults to the device's actual current time instead of a fixed 3:00 - added `getCurrentTimeParts()` (converts `new Date()`'s 24-hour clock to the tool's own 1-12/gozen-gogo shape, correctly handling both midnight→12am and noon→12pm) and a new `resetTimeToolToNow()` that sets the selects/ampm/frame from it, called once at startup instead of the old static default. (b) Layout changed to match Andrew's sketch: the 4 frame buttons now sit in a 2×2 grid on the left (`.time-tool-frames` switched from a wrapping row to `display:grid; grid-template-columns: repeat(2, auto)`), the hour/minute selects stayed in the middle, and AM/PM switched from side-by-side to stacked vertically on the right (`.time-tool-ampm` switched to `flex-direction: column`). (c) Added a thin, full-width "Reset to current time" button (`.time-tool-reset-btn`, deliberately understated - transparent background, thin border, small text, more like a quiet utility action than a primary button) that calls the same `resetTimeToolToNow()` used at startup, so hitting it resets hour, minute, AM/PM, *and* clears any frame back to none - a full return to "what time is it right now," not just a time-value reset. Verified via Node harness across 7 fake "current times" including the two edge cases that are easy to get wrong (00:0x → 12am, 12:00 → 12pm) and a full change-then-reset cycle for each: confirmed the selects/AM-PM button/frame buttons all reflect the real current time on load, and pressing reset after manually changing everything (time, am/pm, and setting a frame) snaps every part of the tool back to "now" with no frame selected.
    - Now v260722.085, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-085` to match.

36. **Two more small refinements from Andrew's screenshot feedback**: (a) AM/PM no longer auto-selects to match the real current time on load/reset - it drew too much attention lighting up on its own. `resetTimeToolToNow()` now always sets `timeToolAmpm = null` regardless of what `getCurrentTimeParts()` computes for it (hour/minute still default to the real current time, just not AM/PM) - matches how real Japanese often drops it when clear from context anyway. (b) The big full-width "Reset to current time" text button that used to sit below the output box is gone entirely - replaced with a small circular icon button (↺, `.time-tool-reset-btn`) folded into the same control row, pushed to the far right via `margin-left: auto` while the frame/entry/ampm groups keep their natural width instead of being stretched apart with `justify-content: space-between`. Net effect: everything sits closer together on one row and the reset control no longer eats a full row of vertical space at the bottom of the card. Card and control padding were also trimmed slightly throughout to fit comfortably. Re-verified the whole tool end to end afterward (not just the delta): defaults, no-ampm-active-by-default across 4 fake clock times, full change-then-reset cycles, and that resetting still correctly restores hour/minute from the real clock while ampm and frame return to off/none.
    - Now v260722.086, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-086` to match.

37. **Two more polish fixes from Andrew's second screenshot**: (a) "Specific time"'s heading sat closer to the card's left edge than "Reading times" directly above it - caused by an inline `padding:0.75rem 1rem` override left over from an earlier compacting pass, which didn't match `.group-card`'s own default padding (`0.875rem 1.25rem`). Removed the override entirely so this card now uses the exact same default padding as every other card, guaranteeing identical left-edge alignment rather than two similar-but-not-quite-matching values. (b) The control row looked oddly left-heavy with one large gap before the reset icon - caused by the previous fix's `margin-left: auto` approach, which bunches the first three groups tightly together and dumps all the leftover space in front of the last item. Switched back to `justify-content: space-between` on the row (removing the `margin-left: auto` from the reset button, no longer needed), so all four groups - frames, time entry, AM/PM, reset - are evenly spread across the full row width the same way the original 3-group layout was, rather than clumped to one side.
    - Now v260722.087, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-087` to match.

38. **Lesson 3 (Level 2) vocabulary added - Transport, UK Places, Places to go, plus extensions**: Andrew sent his classmates' full Lesson 3 word list (Transport, Places in the UK, Places to go, Countries, Time expressions, People/other). Rather than adding it blind, cross-referenced every single word against all 15 existing VOCAB categories first to find genuine duplicates before touching anything. Findings: all 10 Lesson 3 countries already existed in the Countries category (nothing to add there); 5 of 14 time expressions, 3 of 4 people/other words, and 4 of 21 "places to go" words already existed elsewhere in the app.
    - **Three new Level 2 categories added**: `vocab-transport` (Transport), `vocab-places-uk` (UK Places - shortened from Andrew's "Places in the UK" per his request), and `vocab-places-to-go` (Places to go) - confirmed no manual registration list needed anywhere (`buildMatchCategoryList`, `WORD_POOL`, the random-level pool, and "reading words - everything" all iterate `Object.keys(VOCAB)` directly, so a plain new entry in the `VOCAB` object is sufficient to make a category fully functional).
    - **Two existing categories extended**: `vocab-time-l2` gained the 9 genuinely new time words (おととい, あさって, ときどき, せんしゅう/こんしゅう/らいしゅう, せんげつ/こんげつ/らいげつ); `vocab-general-l2` gained ひとりで (alone).
    - **Two items moved, not duplicated, per Andrew's explicit call**: くるま (car) and じてんしゃ (bicycle) moved out of "Everyday items" into the new Transport category, since the course is now covering them there - "car and bike make more sense under the new transport category so bring them forward." びょういん (hospital) moved out of the hidden Extra "trip prep" tier into "Places to go", since it's now real course content rather than only a survival-phrase extra - "it only needs to exist in one place."
    - **Katakana words get a hiragana altForm, deliberately, even though nobody would really write them that way**: Andrew flagged that with 8 classmates now using the app and the course not having reached katakana yet (some know as little as 2 hiragana rows), every katakana word needs a hiragana reading aid via the existing gold "also written:" box. Every katakana (or part-katakana) word added across all three new/extended categories got a hand-verified kana-by-kana hiragana transliteration (e.g. ヘリコプター→へりこぷたー, コッツウォルズ→こっつうぉるず), following the exact same long-vowel-as-ー convention already established by existing entries like ポーランド→ぽーらんど. Words already in hiragana (ふね, あるいて, ちかてつ, でんしゃ, ひこうき, etc.) correctly got no altForm.
    - **Motorbike deliberately added twice**: バイク and オートバイ are both real words for "motorbike" that Andrew wants drilled independently, each with its own hiragana altForm. Checked the two mechanisms this could interact badly with before adding: `pickDistractorItems` dedupes distractor candidates by their *displayed* field value (English in meaning-mode, romaji in sound-mode) - since the two words have different jp/romaji, this can't ever produce two options showing identical text in the same question; `getScoreKey` for vocab items keys on `char + '|' + answer`, so the two get fully independent confidence scores despite sharing an English gloss. Confirmed both empirically with a 13,000-question Node simulation across both drill modes (500 deck builds x 2 modes): zero instances of duplicate-looking option text in any question, in either mode.
    - **Verified end-to-end via Node harness**: extracted and ran `VOCAB` directly - confirmed all 3 new categories exist with the right item counts, くるま/じてんしゃ/びょういん each now appear exactly once and in their new home category (not duplicated, not orphaned), every explicitly-skipped duplicate word (がっこう, ゆうびんきょく, うち, えいが, ともだち, かぞく, だれ, きのう, あした, まいにち, どようび, にちようび) still appears exactly once, `vocab-countries` is untouched at 26 items, and every new altForm hand-check matches the expected hiragana. Total vocab pool: 320 items (was 297 before this addition, net of the 2 relocations).
    - Now v260722.088, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-088` to match.

39. **3 more Lesson 3 verbs added**: いきます (to go), きます (to come), かえります (to return / go home) - all polite -masu forms, added to the existing `vocab-verbs-l2` category (now 9 verbs) alongside おきます/ねます/etc. Checked first and confirmed none were already present anywhere in VOCAB. Worth noting for context, not a duplicate in the app's sense: dictionary-form kanji versions of "go"/"come" already exist in the Kanji section (行/来, from the original 7 general verbs), but those test a different character/form entirely from these polite conversational verbs - same distinction already established between vocab and kanji pools elsewhere (e.g. やすみます vs 休む). All hiragana already, no altForm needed. Verified via Node harness: category now has exactly 9 items, and each of the 3 new verbs appears exactly once across the whole VOCAB object.
    - Now v260722.089, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-089` to match.

40. **いつ (when) moved into "Question & location words"**: Andrew noticed it was sitting in "General vocabulary" rather than alongside なん/だれ/どこ/いくら, and asked whether moving it made sense - it did, so moved (not duplicated). One side effect worth flagging: this also moves its level from 2 to 1, since "Question & location words" is a Level 1 category - いつ is now drilled as part of Level 1 rather than Level 2. Verified via Node harness: "Question & location words" now has 8 items (was 7), "General vocabulary" has 18 (was 19), and いつ appears exactly once across the whole VOCAB object.
    - Now v260722.090, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-090` to match.

41. **New standing convention: a vocab category is filed under the level it was *last* touched by, not the level it started at**: following straight on from moving いつ, Andrew proposed this as a general rule rather than a one-off - since course content only ever grows forward (new lessons keep layering onto existing categories, as happened today), filing by "last used" means a category never needs re-splitting into an early-level part and a late-level part as it picks up newer words. Confirmed this is safe to apply freely: the Level 1/2/Extra pills are a pure browsing filter (`setMatchLevel` just toggles which category tiles are shown, isn't a decision anywhere that gates content), so re-leveling a whole category has zero access-control implications - it only changes which tab it's found under. Applied immediately to "Question & location words" (`vocab-question-location`): moved from `level: 1` to `level: 2` in one step, since it now contains いつ. **This is a standing rule for future vocab additions, not just this one case** - worth applying it by default whenever a new lesson's word gets added to an existing lower-level category, without needing to ask each time.
    - Now v260722.091, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-091` to match.

42. **Level 2 category order reshuffled to teaching order, plus a rename**: Andrew asked for the Level 2 tiles to read general vocabulary → question and location → verbs → transport → places to go → UK places → World cities (renamed from "Cities") → time reference/vocab → days reference/vocab. This is purely an object-key-order change in `VOCAB` (`buildMatchCategoryList` iterates `Object.keys(VOCAB)` directly, and its two special-cased reference-card insertions - the minute-endings card before Time vocabulary, the days-in-order card before Days of the week - are keyed by category name, not position, so they carry through the reorder safely with no code changes needed). No items added, removed, or reworded other than the one label change (`vocab-cities`: `'Cities'` → `'World cities'`). Verified via Node harness: 18 total categories (unchanged), Level 2 `Object.keys` order now exactly matches Andrew's requested sequence, `vocab-cities.label` reads `'World cities'`, syntax-checks clean. Andrew's own check - "anything I've missed?" - confirmed no: the 9 categories he named are the complete set of existing Level 2 categories, nothing left over.
    - Now v260722.092, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-092` to match.

43. **Four place words moved out of "General vocabulary" into "Places to go"**: Andrew spotted that ぎんこう (bank), ぷーる (pool), じむ (gym), and すーぱー (supermarket) were sitting in General vocabulary even though they're places, not general words - moved (not duplicated) into `vocab-places-to-go`, which now has 21 items; `vocab-general-l2` drops to 14 (family, meeting, lunch break, movie, party, Japanese class, work/job, rubbish bin, season, the four meal words, alone - all genuinely non-place vocabulary). Verified via Node harness: item counts correct on both sides, all four words present exactly once in their new home, and a full VOCAB-wide jp+en duplicate scan came back clean (the one match found - シンガポール/Singapore appearing in both Countries and World cities - is a pre-existing, intentional overlap, not something this move introduced).
    - Now v260722.093, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-093` to match.

44. **Version stamp moved from the Read page to Settings**: Andrew asked for this "for next time." Same relocation pattern as item 34's contact-line move - the stamp div now sits directly below "Any problems: Andrew..." at the bottom of `screen-settings` instead of at the bottom of the Read page. Both lines now live together in Settings; the Read page's `screen-match-settings` no longer has anything below the "Start random drill" button. Verified: stamp appears exactly once in the file, in its new spot, full script still syntax-checks clean.
    - Now v260722.094, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-094` to match.

45. **"Export my scores" debug tool added to Settings**, so Andrew can pull real score data off a phone with no dev tools or computer needed - built specifically to let him copy his actual `jpDrillKanaScores` data and share it, to sanity-check the average-based category-swatch idea being discussed (item 44 below) against real numbers rather than theory alone. New `.group-card` on the Settings screen: a read-only `<textarea>` pre-filled with `localStorage.getItem('jpDrillKanaScores')` (defaulting to `'{}'` if nothing's been drilled yet) via a new `populateExportScoresBox()`, refreshed every time Settings is opened (hooked into `showScreen()`'s existing per-screen refresh chain), plus a "Copy to clipboard" button (`copyExportedScores()`) that selects the text and tries `navigator.clipboard.writeText` first, falling back to `document.execCommand('copy')` for browsers without Clipboard API access - either way the text is already selected, so a failed programmatic copy still leaves the user able to copy manually. **Deliberately built visible-but-flagged-as-temporary**: per Andrew's own instruction ("once it's written and used for this we can hide it... use it again for debugging as and when we need it, without everybody seeing this strange box thing"), this follows the exact same hide-not-delete pattern already established for the custom-content toggle (item 29) - it's currently visible so Andrew can use it right now, but is explicitly flagged (HTML comment above the card) to be switched to `display:none` once he's done exporting, with the box/button/JS all staying fully intact underneath for whenever it's needed again. Verified via Node harness: box populates correctly from a mocked `localStorage` (including the empty-state `{}` default when nothing's saved yet), "Copied!" confirmation shows via both the Clipboard API path and the `execCommand` fallback path, full script still syntax-checks clean.
    - Now v260722.095, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-095` to match.
    - **Hidden again in v260722.096** once Andrew had used it to pull his real data (see item 46 below) - `display:none` added back to the card's inline style, box/button/JS all left fully intact, matching the same reversible pattern as the custom-content toggle.

46. **Real score data pulled via the export tool (item 45) and tested against the thematic-consolidation idea - conclusion: consolidation works against the goal, parked**: Andrew exported two datasets (a "laptop" set, mostly early/testing data - narrow vocab coverage, every tested word a perfect first-attempt score - and a "phone" set, genuinely well-populated across 565 keys with meaningful spread). Using the live `VOCAB` object, computed per-category drilled-count and average score for both sets, then applied one candidate consolidation mapping (merging today's 18 categories down into ~10 broader themes - e.g. Countries+World cities, Colours+Trip-prep, Places+Places-to-go+UK Places+Transport) to the phone data specifically. Result: merging categories together measurably destroys diagnostic signal rather than just simplifying navigation - Countries alone was the phone data's weakest category (0.83 avg) but blended with World cities (1.17 avg) it reads as an unremarkable 1.00, hiding exactly the thing a swatch should surface; same effect merging Colours (maxed at 2.00) with Trip-prep (barely touched, 0.00) into a falsely comfortable-looking 1.67. **Conclusion, in Andrew's words: "it sounds like it's going to work against what we're trying to do - even though it felt like a good idea."** The thematic-first *navigation* idea (item 44) isn't necessarily dead - the objection is specifically to physically merging categories' underlying data/scores together, not to showing them grouped under friendlier navigation labels while keeping their individual scores/averages intact. Worth remembering if this gets picked up again: keep category boundaries (and their scores) exactly as granular as they are today, even if the browsing UI ends up presenting them under broader thematic headings.

47. **Small tsu and youon/loanword combining kana rendered visibly smaller**: Andrew felt the font's own subtle size difference for っ/ッ (sokuon) and the second character of combos like きゃ, ファ etc wasn't pronounced enough to read clearly. Added `SMALL_KANA_CHARS` (a set of every small kana: っッ, ゃゅょャュョ, ぁぃぅぇぉァィゥェォ, ゎヮ), `escapeHtmlText()`, and `markSmallKana(text)` - the last wraps any of those characters in `<span class="small-kana">` (new CSS: `font-size: 0.65em`, relative so it scales correctly whether it's sitting in a 100px char-display or a 15px reference row) and escapes everything else, so it's safe to call on any string including plain romaji or ones with no small kana at all (a clean no-op then). Applied at every render site that inserts real Japanese text via `innerHTML` - main drill char-display (word-reading and normal-mode branches), the "also written" altForm line, numbers/time/reversed-mode/kanji-hiragana-toggle option buttons, the vocab reference list (both `jp` and `altForm`), the minute-reference grid, the Specific-time tool's output, and the post-drill missed-characters summary - 12 call sites total. Deliberately left the small preview-blurb text under each category tile (e.g. "がっこう えき かいだん…") untouched, since it's already small/decorative and not where legibility actually matters. Verified via Node harness against real examples with known small kana (がっこう, じゅっぷん - the classic irregular 10-minutes reading, きゃ, ろっぴゃく, いってらっしゃい) confirming correct wrapping, confirmed plain kana/katakana with no small characters pass through unchanged, and confirmed stray HTML-special characters get escaped rather than breaking markup. Full script syntax-checks clean.
48. **Four location words moved from "Places & around the building" (Level 1) into "Places to go" (Level 2)**: Andrew asked whether other categories had genuine destination words that belonged in Places to go, naming school and post office as examples. Audited the rest of "Places & around the building" and moved the four that read as actual destinations rather than generic building vocabulary: がっこう (school), ゆうびんきょく (post office), えき (station), デパート (department store). Left the remaining 15 items in place as structural/generic building vocabulary (office, canteen, reception, meeting room, classroom, toilet, floor, stairs, elevator, escalator, etc.) rather than moving them, since none of those read as a specific place you'd "go to" the way the four moved ones do. One deliberately flagged but not acted on: しょくどう (canteen/cafeteria) is a genuine borderline case (similar in kind to the konbini/restaurant already in Places to go) - **decided, staying put**: Andrew's call was to leave it where it is for now, not worth relitigating one borderline word while the broader category boundaries (see "Thematic-view category boundaries" below) are still actively being reconsidered. Also confirmed for Andrew: すーぱー (supermarket) already exists in the app, added earlier this session (item 43) when it was moved out of General vocabulary into Places to go. Per the standing "file by last-used level" convention (item 41), the four moved words are now Level 2 content, same as every other word in Places to go - flagging this explicitly since these are genuinely basic/early words, not new lesson content, so the level change here is purely a side effect of the topic move rather than reflecting when they were taught. `vocab-places`'s preview `chars` string updated from `'がっこう えき かいだん…'` (referencing two now-removed words) to `'うち じむしょ かいだん…'`. Verified via Node harness: 18 categories total (unchanged), Places now 15 items (was 19), Places-to-go now 25 items (was 21), all four moved words confirmed present in their new home, full VOCAB-wide duplicate scan clean (aside from the known pre-existing Singapore country/city overlap).
    - Now v260722.097, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-097` to match.

49. **"Read" nav pill/screen renamed to "Vocab"**: Andrew noted the other three main nav pills (Hiragana, Katakana, Kanji) are named after content type, while "Read" was named after a drill mechanic instead - one that covers reading-comprehension questions, category browsing, *and* drilling, not just passive reading. Renamed the outer nav pill (`#nav-match`, both its initial HTML text and the `showScreen()` code that restores it after leaving a sub-screen) and the screen's own title (`settings-title`) from "Read" to "Vocab", plus two descriptive code comments for consistency. **Deliberately left two things unchanged**: the inner level-filter-row pill inside this screen (`#level-pill-read`, `setMatchLevel('read')`) still says "Read" - that one specifically means "random reading challenges" as opposed to browsing Level 1/2/Extra, which is still an accurate, unambiguous label now that the outer nav pill says something different; and the "Reading numbers" / "Reading times" / "Reading words —..." card names stay as-is, since those genuinely describe reading-type activities rather than the section as a whole. Verified via grep that only the intentional inner "Read" pill remains, full script still syntax-checks clean.
    - Now v260722.098, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-098` to match.

50. **"Show custom content" toggle properly fixed and re-enabled; redundant "Reading words — L1/L2" pool removed** (backlog items #1-2 from the previous message, actioned same session): the toggle's real saved value now actually takes effect - removed the force-on override line that had been unconditionally setting `appSettings.showCustomContent = true` on every load regardless of what was saved (added back in item 29 as a stopgap), and un-hid its Settings card (`display:none` removed). Default remains `true` (unchanged, set in the `appSettings` object literal itself), so - per Andrew's explicit requirement - nobody notices any difference unless they actually visit Settings and switch it off. Fixed the real bug behind it: `startRandomMatchEverything()` (backs "Reading words — everything") now skips any category with `level: 'extra'` when the toggle is off, matching every other Extra-gated entry point (Kanji nav pill, Extra level pill, Random kanji card) instead of unconditionally pulling from all 18 categories including Colours and Trip-prep. With that fixed, "Reading words — from Level 1 and Level 2" became truly redundant (once "everything" properly excludes Extra when asked to, "L1+L2 only" and "everything" are the same set) - removed entirely: the HTML card, the `'l1l2'` branches in `selectReadRandomPool()`/`startReadRandomDrill()`, and the now-dead `WORD_POOL` constant plus `buildWordReadingDeck()`/`startWordReading()` functions. Verified via Node harness (extracting real functions against a mocked DOM/localStorage, not just reading through): confirmed the removed force-on line is gone and the true default is `true`; confirmed toggling off drops `startRandomMatchEverything()`'s pool from 323 items to 287 (exactly the 36 Extra-tier items - Colours' 10 + Trip-prep's 26 - removed) and toggling back on restores all 323; confirmed all three gated UI elements (`nav-kanji`, `level-pill-extra`, `read-pool-kanji`) plus the toggle card's own `.selected` state all flip correctly together; confirmed `toggleCustomContent()` genuinely persists to `localStorage` now (real bug in the earlier ad-hoc test harness - not the app - initially masked this until a missing `SETTINGS_KEY` in the test setup was found and fixed). Full script still syntax-checks clean; grep confirms zero remaining references to `WORD_POOL`, `buildWordReadingDeck`, `startWordReading`, or `l1l2` anywhere in the file.
    - Now v260722.099, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-099` to match.

51. **Existing Numbers/Times score-swatch changed from rectangular to square, as a visual trial before committing to swatches elsewhere**: Andrew felt the current swatch (`flex: 0 0 30%` width + `align-self: stretch` height, so its shape followed the card's width and text height uncontrolled) reads as slightly dominating on screen. Changed to a fixed `48px × 48px` square (`flex: 0 0 auto; width: 48px; height: 48px`), dropping the percentage-width and stretch-height so it no longer scales with the card at all - it now just sits at a fixed size, vertically centred via the card's existing `align-items: center`. Purely a CSS sizing change - the colour-class rules (`.score-swatch.score-neg2` etc) are untouched and still apply the same way. Verified nothing else in the file depends on the old sizing (no JS reads the swatch's dimensions). **Confirmed by Andrew after seeing it live - keeping the square size.** His original worry: on a full screen of stacked cards, a whole right-hand third coloured on every one of them risked feeling visually heavy when seen all together, not just on one card in isolation. The square reads as noticeably less dominating, so this size is settled. Still separately undecided: whether/when to actually add swatches to category cards more broadly (that's the still-open "should we build this now" question from a few messages back, not resolved by this sizing trial alone).
    - Now v260722.100, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260722-100` to match.

52. **Category confidence swatch rolled out to the category-select cards (Level 1/Level 2/Extra)** - Andrew's "ready to roll out to the category cards now," following straight on from item 51's square-swatch trial and the design worked out across items 157/160/161/171/172 below. Implements just the confidence-tint half of that design (the level border/badge idea from item 160 was deliberately left out - it was already flagged as having no practical value until By theme exists, since no current tab mixes L1/L2 categories together, and Andrew's ask was specifically about the swatch).
    - **`getCategoryAverageScore(categoryKey)`**: averages a category's per-word scores (same `jp + '|' + en` key `getScoreKey` already uses for vocab), excluding any word that's never been drilled (not in `kanaScores` at all) rather than counting it as neutral/zero - matches the never-drilled-excluded convention already settled in items 171/172. Returns `null` if literally nothing in the category has been touched yet, so an untouched category shows no tint at all (same "never drilled = no shading" rule `kanaScoreClass` already follows for single items).
    - **`categoryConfidenceClass(avg)`**: a new 7-band scale (`cat-conf-neg3/neg2/neg1/neutral/pos1/pos2/pos3`), wider than the 5-band per-word scale, since a whole-category average smooths toward the middle and a coarser scale would make most categories look identical - matches the "wider range of shades" idea from item 170/171. Thresholds at ±0.3/±0.8/±1.4 within the existing -2..+2 score range. New, distinctly-named class family (`cat-conf-*`, not `score-*`) so it can never collide with the existing per-word/per-pool score classes if both ever needed to coexist on related elements.
    - **Applied as a full-card background tint** directly on `.match-card-select`/`.group-card` (reusing the same red/green rgba hue families as the existing swatches, just with 3 opacity steps per side - 0.15/0.30/0.45 - instead of 2), matching the `.kana-ref-card` full-tint convention rather than the boxed `.score-swatch` side-block one, per Andrew's correction that these cards have no spare interior space for a separate element.
    - **Wired into three places** so it actually stays fresh: (1) `buildMatchCategoryList()` computes and applies the class inline at card-creation time, since it already fully rebuilds the Level 1/2 list from scratch on every call; (2) the two static Extra-tier cards (Colours, Trip prep) aren't rebuilt dynamically, so they got a `data-key` attribute added to their HTML and a new `refreshExtraCategorySwatches()` function, called both at startup and whenever `setMatchLevel('extra')` runs; (3) **the real fix, found by tracing the return-from-drill path rather than assuming it worked**: `showScreen()`'s existing `match-settings` branch only called `updateNumberTimeSwatches()` on return, never anything that would refresh the category list - so a swatch would have stayed stale until the user happened to flip level tabs. Added `buildMatchCategoryList()` and `refreshExtraCategorySwatches()` to that same branch, so returning from a finished drill immediately shows the category's just-updated colour.
    - **Verified via two Node harnesses**: (1) pure logic - `getCategoryAverageScore`/`categoryConfidenceClass` extracted and run against constructed `kanaScores` fixtures, confirming never-drilled items are correctly excluded from the average (not counted as zero), an all-untouched category returns `null`/no class, and all 7 band boundaries classify correctly. (2) DOM-level - `buildMatchCategoryList`/`refreshExtraCategorySwatches`/`setMatchLevel` extracted verbatim from the real file and run against a minimal fake DOM (real `classList`/`dataset`/`querySelectorAll` semantics): confirmed a Level 1 card gets the correct class at build time, `refreshExtraCategorySwatches()` correctly clears a stale class before applying the current one (and leaves an untouched category with no class), and `setMatchLevel('extra')` triggers the Extra-tier refresh. Full script syntax-checked clean afterward.
    - Now v260724.101, `sw.js`'s `CACHE_VERSION` at `jp-drill-v260724-101` to match.

## Parked ideas (discussed, not built - no code changes yet)
- ~~Show-custom-content toggle doesn't actually keep Extra-tier words out of the random reading pools~~ — **resolved, see item 50**: `startRandomMatchEverything()` now correctly excludes the Extra tier when the toggle is off, the toggle is re-enabled (default on) and its real saved value now takes effect, and the redundant "Reading words — L1/L2" pool (along with the now-dead `WORD_POOL`/`buildWordReadingDeck`/`startWordReading`) has been removed entirely.
- **Brief release notes, hidden in Settings, keyed to the version stamp**: the other idea that originally motivated wanting a settings home. Not yet designed or built - Andrew wants to think about what "very brief" should actually look like before this gets built.
- **Merge Countries and World cities into one "Around the world" category, split visually by a horizontal gold divider (countries above, cities below)**: new idea from Andrew, not yet designed. Singapore stays listed as a country (its existing overlap with World cities - Singapore appearing in both today - isn't being removed by this). Worth reconciling against item 46's conclusion when this gets picked up: that finding was specifically about merging *scores* together losing diagnostic signal (Countries' real weak spot got hidden inside a blended Countries+Cities average) - this idea might be fine if it's a *visual/label* merge only (one card, one browsing entry point, gold line as a visual section break) while each half keeps its own separate score underneath, but would reintroduce the same problem if the two are merged into one scored unit. Needs deciding which one Andrew means before building.
- ~~Toggle between level-based and thematic drilling, on the Settings page~~ — **superseded, see "By theme" navigation shape below**: Andrew resolved this into something simpler than a toggle - a fifth pill instead, so both stay permanently available rather than needing a mode switch.
- **Reconsider what the "Drill" nav pill is for, possibly splitting it in two**: new idea from Andrew - e.g. random drills on one pill, thematic/level-based drills on another, freeing up or repurposing the space rather than cramming both under one "Drill" entry point. Also ties into the item 44 navigation discussion (the "random tasks vs. browse tasks" split floated there). **Investigated, not yet built**: confirmed `nav-drill`'s only job is `tryResume()` - if a deck is mid-drill it jumps straight back in, otherwise it falls back to `goBackToSettings()` (returns to whichever selection screen a drill was last started from); it also happens to be the pill that lights up "active" for the drill/results screens via `showScreen()`'s catch-all branch, since neither of those has its own pill. So it isn't a genuine content destination like the other four - it's a resume shortcut plus a highlight anchor. Both jobs look replaceable without a dedicated pill: resuming could happen automatically (parallels the existing sessionStorage-based drill-resume-after-reload mechanism), and the active-highlight doesn't strictly need a pill lit at all while genuinely mid-drill. Also confirmed: `.nav-pills` already has `overflow-x: auto` (scrollable, hidden scrollbar) built into its CSS, unused so far since 5 pills always fit - worth checking whether this alone is enough before assuming a purpose-built scrolling pill row is needed for future pill growth.
- **"By theme" navigation shape, worked out in detail with Andrew - not yet built**: resolved the earlier "Settings toggle between level and thematic drilling" idea into something simpler - no toggle at all, just a fifth pill added to the existing Read/Level 1/Level 2/Extra row: "By theme". Every mode stays permanently available side by side, no mode to pick or remember - "every way is available all the time and no choice to make" (Andrew's phrasing). Confirmed this fits the earlier presentational-merge conclusion cleanly: By theme would just be a different arrangement of the same categories and the same per-category scores, nothing structural changes underneath - a category's swatch would look identical whichever tab was used to reach it. Two follow-on decisions, also worked through: (1) tapping a themed category goes to the *existing* per-category selection screen (`showVocabTileSelection()` → `screen-vocab-tile-selection`, already shared by Level 1/Level 2 today) rather than a new screen or a jump-straight-into-a-random-20-drill flow - the existing "Select all" button gets a new sibling, "Select random 20," so manual selection, select-all, and random-20 all live inside that one shared screen regardless of which tab (Level 1, Level 2, or By theme) got you there. This also means By theme's own page needs no cross-category "Random reading" button the way Level 1/2 have at the bottom of their lists - the randomness lives inside each category instead, which maps naturally onto the level-vs-theme purpose split already settled (level = "test me on everything so far," theme = "test me on this one topic"). (2) "Select random 20" should be disabled/hidden for any category with fewer than 20 items (nothing to randomly reduce otherwise) - Andrew's expectation is this mostly matters for today's smaller *level* categories (Clothing at 4, UK Places at 7, Days at 7, Question & location at 8, Verbs at 9) rather than future themed ones, since merged themes will likely mostly exceed 20 items on their own. Minor practical note flagged, not a blocker: the level-filter-row's CSS has no wrap/scroll today, so a fifth pill may need small adjustments (padding/font-size trim, or wrap/scroll) once actually built. **Fully theoretical still - nothing built, no code changes.**
- **Level badge for thematic view - small circled marker, blue "1" / orange "2" / and now "X" for Extra**: Andrew's proposed answer to how a category's level stays visible once By theme mixes L1 and L2 categories together - a small circled badge in the corner of the card, colour-coded by level. Purely a display read of each category's existing `level` field, no new data needed. Extra tier gets its own "X" badge (own distinct colour, not reusing blue or orange) rather than no badge at all - decided in favour of consistency, so the badge always tells you a word's origin (L1/L2/Extra) regardless of which tab surfaced the category. Andrew also noted in passing that today's smaller categories will likely grow past the under-20 threshold naturally over time anyway - Days of the week once months get added, Clothing once the course covers more of it (at which point, per the standing "file by last-used level" rule, it'd shift from L1 to L2).
- **New Extra-tier "[UK] cities" list proposed - roughly the 15 biggest UK cities, distinct from the existing "UK Places" (L2) category**: motivation is extending real-world sentence scope (e.g. "Andrew-san is going to Brighton by bus on Monday") without teaching new vocabulary - transliterated place names are just katakana/hiragana renderings of already-known English city names, not new vocabulary in the way a new verb or noun would be, so the pedagogical cost of adding many of them is low. Fits the Extra tier's existing purpose (optional, not part of the taught course) well. Worth being careful about naming once built - there'd be three related-but-distinct things (taught "UK Places" landmarks at L2, "World cities" at L2, and this new Extra-tier UK cities list) that could easily get confused without a clearly distinguishing name. Would need the same hiragana altForm treatment as other katakana-heavy content this session, for the same reason (course hasn't reached katakana yet).
- **Card real-estate reservation (raised two messages ago) resolved with a border/background-tint pairing**: Andrew pointed out the corner-badge idea has a real problem - Japanese preview text on category cards runs long and unpredictably, so a badge tucked in a corner can end up feeling disconnected from whichever text it's meant to describe. His proposed fix for the level marker: use the card's own border colour instead of a badge - a border wraps the whole card, so there's no ambiguity about what it applies to, and it costs zero interior space. Default border colour would be level-specific (blue/orange/Extra's own colour), switching to gold on selection exactly as today's `.selected` state already does - confirmed this is a clean, non-disruptive addition since the existing selected-state override already takes priority over whatever the resting border colour is. Extending the same logic to the confidence swatch (since it needs a different carrier now that the border's spoken for): a subtle full-card background tint, reusing the same soft rgba colours the Numbers/Times swatch already uses today, just painted across the whole card instead of held in a separate side block - also zero layout space, also doesn't compete with long text since it's meant to read as a glanceable tint rather than a legible label. Net shape: border colour = level (gold when selected), background tint = confidence. **Confidence-tint half now built, see item 52 above** (full-card background tint on `.match-card-select`/`.group-card`, 7-band `cat-conf-*` scale). **Border/level-badge half still not built** - remains parked until By theme actually exists, since no tab today mixes L1/L2 categories together for a level marker to disambiguate.
- **Readiness check (Andrew asked directly, worth preserving the answer)**: the category swatch and the level badge are both buildable independently right now - neither depends on the theme-naming question, since the swatch just averages existing per-word scores and the badge just reads each category's existing `level` field. The actual "By theme" page is the one piece still genuinely blocked, since it can't be built before the theme groupings/names themselves are settled. **One concrete reservation raised, not just a restatement of open items**: the level badge and the swatch will likely both want to live on the same small category card (a coloured corner badge plus a coloured confidence indicator, alongside the existing label/count/preview text) - worth designing those two together even if built in separate passes, rather than bolting them on sequentially and risking a cramped, over-coloured card that needs reworking. Recommendation given: green light to start the swatch + level badge as a considered pair; By-theme itself still waits on the theme-naming decision. **Swatch half actually built in item 52** (Andrew asked for the swatch specifically, not the pair) - the level badge was deliberately not added alongside it, on the basis that it has no practical value until By theme exists.
- **General "reference-only" pages (no drilling) - new idea, not designed, explicitly not urgent**: Andrew noticed the app already has two of these (minute endings, days-of-the-week-in-order), both fixed lookup content rather than quizzable vocabulary, and floated that similar treatment could suit Months (once added) and, more ambitiously, a sentence-structure diagram his teacher gave the class this week - "I wouldn't want to drill grammar unless you're up for a really big challenge." Two distinct sub-cases worth keeping separate when this gets picked up: something like Months would be nearly free to add, since it'd just become another VOCAB category with one more special-cased reference-card insert, exactly mirroring how the existing Days-of-the-week reference already works (proven, cheap pattern). A grammar/sentence-structure diagram is a different kind of build entirely - today's reference screen is a two-column word list (`showVocabReference`/`vocab-ref-row`), not built for diagrams, boxes, or arrows, so that would need genuinely new UI rather than reusing the existing list renderer. **Confirmed with Andrew**: rather than anchoring new non-vocab references in front of an unrelated category the way Minutes/Days-order do today, they should get their own standalone "References" section/home - matches what he'd already been thinking. Still not designed or built - just the destination is settled, not the mechanism, layout, or where it sits in navigation. Flagged as useful "at some point," not immediately.
- **Andrew's overall read: mechanics are converged, themes (and separately, Numbers) are what's left**: the fifth pill, the shared-selection-screen-plus-random-20-button flow, and the level badge all feel settled conceptually now. What's left hanging is (a) finalising the actual theme groupings/names (the "Places & travel" naming problem) and (b) the separate, not-theme-related Numbers question from earlier - flagged explicitly since it's easy for Numbers to quietly get swept into "it all hangs on themes now" when it's really its own independent decision.
- **Thematic-view category boundaries - still being actively rethought, nothing settled**: Andrew doesn't love the "Places & travel" merge candidate from the earlier theme-count discussion - mixing cinema, stairs, car, and ship under one label "feels a bit random." Landed on an interesting resolution mid-conversation rather than immediately restructuring: the incoherence may be a *naming* problem more than a *content* problem - the right label supplies the frame that makes a mixed list read as intentional (e.g. "out and about" could comfortably cover buildings, destinations, and transport together, where "Places & travel" didn't earn that same coherence). Test proposed: can the frame be said as one natural sentence a learner would recognise as true? If yes, the grouping probably holds regardless of how different the items look side by side; if you're straining for the sentence, that's the real signal to split rather than rename. Softens (doesn't eliminate) the earlier three-way-split suggestion (Getting around / Places to go / Around a building) - a good name might rescue the original single grouping instead. **Nothing decided - Andrew wants to keep thinking rather than pick a name or a split tonight.**
- **Whether "Numbers" (Level 1, 40 items) is still needed as its own drillable category, given the random "Reading numbers" challenge already exists**: Andrew's instinct is that once a class can reliably do the random-number challenge (which requires composing arbitrary numbers on the fly, e.g. reading 3,482 as さんぜん+よんひゃく+はちじゅう+に), the standalone 40-word atomic-vocabulary drill might be testing something already proven redundant - success at the harder compositional test implies the easier atomic-recognition one is already covered. Real distinction exists between the two (atomic word recognition vs. compositional reading of arbitrary numbers), but whether that distinction is still *useful* to keep drilling separately depends on how the actual class is doing, which isn't something the code can answer. Andrew proposed a threshold test for when full thematic-only (no Level 1/2 split at all) would make sense: "if it was 250+ I could justify it, but we're not there yet" - **worth flagging: Level 1 (163) + Level 2 (124) already total 287, past that mark**, though Andrew may be thinking per-theme rather than in raw total, or setting the bar higher than he'd first assumed - not yet reconciled. **Nothing decided - explicitly parked ("I need to think about this one").**
- **What level-based drilling is actually *for*, once thematic browsing exists - clarified, not yet built**: worked through with Andrew rather than just asserted. Level answers "am I solid on everything taught so far" (a course-stage-scoped question); theme answers "am I solid on this specific topic" - genuinely different, not redundant. Confirmed along the way that today's "Random reading" *for a level* has always meant a random 20 sampled from that level's pool, never the complete set in one sitting - so level-random and theme-random already share the exact same underlying mechanism (sample 20 from whichever pool of categories is currently selected); the only real difference is how the pool is scoped (by course stage vs. by topic), not how the sampling works. Andrew's own proposed shape: thematic drilling stays bounded to random-20s (matching how it already works everywhere else), levels remain available specifically for cases wanting the course-stage-scoped question. **Conceptually resolved as a division of labour; not yet designed as an actual settings/UI mechanism (see the level/thematic Settings toggle idea above).**
- **Extending the numbers/times aggregate-consistency swatch pattern to other reading pools**: Andrew likes having a score-driven colour indicator on "Reading numbers"/"Reading times" (already works via a single running score per category, nudged ±0.1 per answer regardless of pool size - not a percentage-of-items-mastered metric, which is why it already sidesteps the "variable category size" problem he was stuck on). Confirmed the same mechanism would transfer directly to other pools (Reading words L1/L2, Reading words everything, Random Kanji, or individual vocab categories) with zero new metric design needed - just a new score key per pool, same ±0.1 nudge. Andrew explicitly wants to avoid any "here are your weak points, focus on those" framing, but likes the idea of surfacing genuine blind spots (e.g. having clearly forgotten an entire theme like place names) more passively via colour. He also likes that overlapping keys (a specific category's score + a broader pool's score both moving from the same wrong answer) create a small multiplier effect that adds incentive to get things right. Left with Andrew to decide which pools/cards he actually wants this on before any code changes. **Superseded/complicated by the item directly below** - Andrew has since flagged that this doesn't scale to topic categories the way it does to Numbers/Times, which never change shape.
- **Whole-section rethink of the Read page's vocab navigation - thematic-first instead of level-first, discussed but not designed or built**: Andrew named the current navigation "clunky" - specifically the mismatch between the always-random tasks (Reading numbers/times, Specific time, L1+L2 words, everything, Random kanji - currently all living under the "Read" pill) and the browse-a-category tasks (currently forked across separate Level 1/Level 2/Extra pills). Proposed direction: flip the primary axis from Level to Topic - every thematic category (Greetings, Transport, Time vocabulary, etc.) shown in one place, with its Level shown as a lightweight visual cue on the card itself (e.g. a border colour or small tag) rather than as a tab you have to pick first. This would also retire the "file a category under whichever level last touched it" standing rule (item 41) entirely, since a category's level would just be a decoration, not a structural fork - it would never need re-filing again as new lessons extend it. Likely pairs with keeping "random/mixed challenges" as its own separate top-level mode, since those aren't really topic categories at all (always-random, never individually browsable) and probably shouldn't be squeezed into the same thematic list.
  - **Related, flagged as its own open problem rather than solved**: the single-running-score swatch idea (previous bullet) doesn't scale to topic categories the way it does to Numbers/Times, because those two pools are the only ones with a fixed, never-changing shape. A topic category like Transport or Places to go keeps growing lesson over lesson (13→more items, 21→more items), which breaks a single blended score two ways: newly added words silently ride on the score already earned by the old ones (so a category can look "known" before any of its newest words have ever been tested), and a single number gets less informative the bigger/more mixed a category becomes - it stops being able to say anything as specific as "you know last month's words but not this week's," which is arguably the more useful signal. Possible directions (none decided, no clear free option): splitting scores by when a word was added (re-creates the same fragmentation-by-time-period problem the thematic reshuffle above is trying to escape), decaying older answers so new words naturally pull the average down without needing a hard reset (adds real complexity to what's currently a simple ±0.1 nudge), or accepting the swatch is inherently a coarse "have you engaged with this" signal rather than a mastery percentage and not trying to make it do more than that. Andrew's call whenever this gets picked back up - deliberately not designed further tonight.
  - **Follow-up proposal from Andrew, still theoretical, addresses the scaling problem above directly**: instead of ever drilling a whole large merged category (e.g. a future combined Places+Transport at 47+ items) in one go, always drill a random sample of 20 regardless of category size - same "Random reading" sampling mechanism already used elsewhere, just applied as the default way to engage with any category. Score nudge becomes ±0.05 per answer so a perfect 20-item round nets exactly +1.0 - decouples the score's per-round swing from category size entirely (today's flat ±0.1-regardless-of-pool-size nudge means a round's total swing scales with round length, which is the root of the scaling problem). For manual word selection: fewer than 20 words picked still scores flat 0.05/word (so a small manual selection can't reach a full point); 20 or more picked scores proportionally, (correct/total)×1, so even a 73-word selection caps at 1.0. Also wants more colour shade bands (0.5/1/1.5/2, up from today's coarser buckets) and bolder/less pale swatch colours specifically in the light theme. I flagged two open questions back to Andrew rather than picking an answer: (1) a single unified formula (score = net correct ÷ round size, whatever the round size actually was) would be simpler than the two-tier flat-under-20/proportional-over-20 rule and would naturally handle any round size, but it also lets someone farm full points by manually selecting just 1-2 already-known words, which the two-tier rule was deliberately guarding against - worth deciding which property matters more; (2) several categories today are already under 20 items on their own (Clothing 4, UK Places 7, Days of the week 7, Question & location words 8, Verbs 9) and can never assemble a random-20 round at all - under the flat-0.05-manual rule a perfect run through all of Clothing caps at just 4×0.05=0.20, meaning a small category could never show full confidence colour no matter how well-known it is; may resolve itself if thematic merging folds most small categories into bigger ones, but worth deciding deliberately for whichever stay small (Clothing looks likely to stay small on its own). Nothing built - Andrew is still thinking.
  - **Simpler direction found, likely supersedes the round-based formula above**: rather than inventing a new round-scoring mechanic for categories (fixed-20 sampling, the flat-under-20/proportional-over-20 split, etc.), the category swatch could just be a read-only average of each member word's own already-existing per-word score (every vocab word already gets its own persistent score via the same `kanaScores` mechanism kana/kanji use - no new scoring state needed at all). This dissolves both open problems from the round-based idea as side effects rather than needing separate fixes: it's automatically size-independent (a 47-word category's average behaves the same way regardless of size), and it's naturally gaming-resistant (farming 1-2 already-known words barely moves a big category's average, unlike a round-based full-point score). A wide band of shades (rather than today's 5 buckets) suits this well too, since reaching absolute red/green requires nearly every word in the category to be simultaneously at the extreme - one missed word pulls the whole average back toward the middle, which is a much more forgiving/nuanced signal for a big category than it would be for a single kana character.
  - **Confirmed with Andrew: never-drilled words are excluded from the average, not counted as neutral/red** - a word only enters a category's average once it actually has a score (been answered at least once). Known consequence, discussed and accepted rather than engineered around: a brand-new large category with only a handful of words touched so far can show a misleadingly confident (or unfairly harsh) average off a small sample, until more of the category gets drilled over time and the average naturally smooths out. Andrew's view: this only matters for genuinely new categories starting from zero, and smoothing out over time is an acceptable behaviour rather than something to add a minimum-coverage safeguard for. Still entirely theoretical - nothing built.

## Known outstanding items (deliberately not built yet)
1. ~~Vocab distractor plausibility~~ — **resolved, see item 17**: `pickDistractorItems` now biases toward confusable candidates (matching first letter, similar length) instead of pure-random. Measured 19%→98% improvement in "at least one same-first-letter distractor" rate. If this still doesn't feel like enough in practice, the fallback plan (expanding toward the N5 standard ~650-850 word set, not pulling in outside words) remains on the table.
2. ~~Kanji's remaining invented "flavour" distractors~~ — **checked, fully resolved**: audited three ways (no kanji/survival DATA item still carries a static `options` field or any field beyond char/answer/disp; no leftover `hint` fields; all 54 `KANJI_COMPOUND` example words are backed by a real `KANJI_READINGS` entry, not invented). Every kanji distractor now goes through `buildKanjiOptionsFromPool`, which only ever draws from real pool items — no remaining code path could inject a made-up option.
3. **Browser back-button reliability — PWA manifest experiment done, hypothesis disproven, real cause found**: tested in a separate chat (kept canon safe), manifest-only (no service worker) — 12 clean lines added to `<head>`, installs to home screen fine, opens chrome-less. **Result: doesn't fix it.** Going back during a drill still resets it to the beginning even with no browser tab underneath, so "no tab = no gesture to fall into" wasn't the real cause. Re-diagnosis: the existing history-trap (`history.pushState({trap:true})` + re-push on `popstate`, near `</body>`) only re-pushes a dummy state and never resets anything itself — no `visibilitychange`/`blur`/`pagehide` handler exists that would explain a deliberate reset either. **Actual cause (best explanation)**: drill state lives only in JS variables, with no history entry per screen. The OS back gesture backgrounds the app, Android/the WebView kills the page process to reclaim memory, and reopening is a cold reload of `index.html` — indistinguishable from "reset to beginning" because the old JS instance is simply gone, not because anything actively wiped it. Confirms a service worker (offline caching) wouldn't help either — it only affects how network requests are served, not in-memory JS state; faster/offline reload doesn't save a drill in progress.
   - **Manifest itself**: low-risk, does what it says (home-screen install, no browser chrome) — just doesn't solve the back-button problem. Not yet merged into canon; Andrew's call whether the install experience is worth keeping on its own merits regardless.
18. **Drill-resume fix built and verified in sandbox (later corrected in item 20 after real-device testing found sessionStorage didn't survive Andrew's actual back-button behaviour), addressing item 3's real cause**: rather than trying to prevent the OS from killing the backgrounded WebView (not really preventable from page JS), the drill's own state now survives that kill. `renderQuestion()` calls a new `saveDrillSession()` on every question shown, mirroring `deck`/`current`/`score`/`wrong`/`hints`/`missed`/`missedItems`/`lastSettingsScreen`/`drillByMeaning`/`reversed`/`showHiraganaOnKatakana`/`showHiraganaOnKanji` into `sessionStorage` (one call site, since all 5 separate drill-start functions already funnel through `renderQuestion()` - this is why the fix is a single ~3-line hook rather than touching every drill type individually). `showResults()` clears it on genuine completion. On page load, `tryRestoreDrillSession()` checks for a leftover session and offers a native `confirm()` to resume (deliberately not a custom banner - zero new HTML/CSS, so it can't interact badly with any existing screen). One known nuance, flagged rather than hidden: the deck-builder closures used for "Drill again" aren't JSON-serializable, so a resumed session's "Drill again" reshuffles the same restored deck instead of rebuilding fresh distractors — reasonable rather than leaving it broken, verified working. Verified end-to-end via Node harness with a shared sessionStorage mock across two completely separate script executions (a true simulation of "process killed and reloaded," not just a function call): state before the simulated kill (current=1, score=0, wrong=1, on kanji-selection, mid-drill in the Places section) was restored *exactly* after a fresh load - same index, same score, correct current character with fully populated options. Also verified: no prompt at all when there's nothing to resume; declining the resume prompt correctly discards the session; finishing a drill normally clears it so a later reload doesn't ask about a stale one; "Drill again" after a resume produces a working fresh deck. The whole change is contained to one new self-sufficient block plus three single-line hooks into existing functions - nothing else touched.
4. ~~PWA~~ — **merged into canon, see item 24**: manifest-only (no service worker) confirmed on Andrew's phone to not fix the back-button issue, but kept anyway for the cleaner chrome-less install experience. One known real limitation on Android specifically: installing doesn't place an icon directly on the home screen, it lands in the app drawer like any other installed app, and pinning it to the home screen is a separate manual step - not something a manifest can control. Andrew is checking classmates' phones to gauge real-world appetite before deciding whether it's worth publicizing to the group. Service worker itself: still not pursued, stale-cache risk assessment unchanged.
5. ~~School/bank/shop compound-duplicate question~~ — **resolved, see item 22**: rather than promoting the duplicate-gloss compounds into separate cards, they now show via the existing gold "also written:" box (the `altForm` field, already used by vocab). Extended to all 7 genuine word-for-word duplicates found in the data (校/店/行/火/水/木/日), not just the original 3.
6. ~~"Drill all verbs I know" vs. thematic placement~~ — **resolved, see item 15**: Verbs is now a computed cross-section pool via `getKanjiSectionPool`, surfacing every "to ___" verb from wherever it lives with zero duplication in the underlying data.
7. ~~Travel section has grown too large to drill comfortably~~ — **resolved, see item 13**: rebalanced by merging Weather into Nature and carving out a new Places section. Travel is now 44 items (was 63).
8. ~~`missed` tracks by bare character, not character+meaning~~ — **resolved, see item 16**.

## Content/pedagogical notes worth knowing
- Andrew is ahead of his own class's pace (drilling katakana fully while classmates are still on hiragana's vowel row) — kanji section is currently "his alone," which he's treated as a low-stakes opportunity to experiment more freely with kanji-specific fixes, since nothing else depends on it yet.
- Removing shape-matching (kanji glyph no longer shown in options) and retiring the old matching-pairs game were deliberate difficulty increases over time — Andrew has explicitly noticed and named this pattern ("my crutch has been taken away") and is treating it as an open, honest question (harder-but-more-honest vs. just harder) to observe via actual use, not something to reverse.
- **Why Vocab and Kanji are organized so differently**: Vocab's categories come directly from the actual lessons taught in the in-person course — its structure mirrors how the curriculum itself grouped things (e.g. a dedicated Verbs lesson/category exists because the course taught verbs together). Kanji started from a completely different brief — "give me a set of kanji useful for travel, my trip, and survival," which then expanded into "give me N5" (full JLPT N5 coverage) — and its thematic sections (Travel, Food, etc.) were a categorization layer applied afterward, not inherited from a lesson plan. This is why kanji verbs ended up scattered thematically (open/close/push/pull in Travel, eat/drink in Food, only the leftover general-purpose ones in Verbs) while vocab verbs didn't — it's two different provenances behaving sensibly for their own origin, not an inconsistency needing a fix.
